/*
const games = {
    game1 : 'GTA',
    game2 : 'COD',
    game3 : 'NFS',
    game4 : 'FH',
}

for (const key in games) {
    const element = games[key];
    console.log(element)  
}
*/

const languages = ["JavaScript", "Python", "Swift", "R", "C++", "C#"];
for (const key in languages) {
  const element = languages[key];
  console.log(element);
}
